# user-list-app
1. Clone the repo.

2. Install TypeScript globally (optional if you have global `tsc`):
   
npm install -g typescript

3. Compile TypeScript to JavaScript:

tsc src/main.ts --outDir dist --target ES2017 --lib DOM,ES2017

4. Open index.html with Live Server (VS Code extension) or any local server.
